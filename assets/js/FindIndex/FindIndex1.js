// Ejemplo 1: Encontrar el índice del primer número par en un array
const numeros = [1, 3, 5, 7, 8, 9, 10];
const indicePrimerNumeroPar = numeros.findIndex((numero) => numero % 2 === 0);
console.log(indicePrimerNumeroPar);
// Resultado: 4
