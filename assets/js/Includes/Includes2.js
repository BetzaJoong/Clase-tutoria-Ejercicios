// Ejemplo 2: Verificar la existencia de un elemento a partir de un índice inicial
const numeros = [1, 2, 3, 4, 5, 4, 3, 2, 1];
const existeTres = numeros.includes(3, 3);
console.log(existeTres);
// Resultado: true