// Ejemplo 1: Verificar si algún número es par en un array
const numeros = [1, 3, 5, 7, 8, 9, 10];
const algunNumeroPar = numeros.some((numero) => numero % 2 === 0);
console.log(algunNumeroPar);
// Resultado: true